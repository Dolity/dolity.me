---
import BaseLayout from '../../components/layout/BaseLayout.astro';
import NowPage from '../../components/now/NowPage.astro';
import { t } from '../../i18n';

export const prerender = true;

const loc = 'th';
---

<BaseLayout
	title={t(loc, 'now.title')}
	description={t(loc, 'now.lead')}
	seoPath="now"
>
	<NowPage />
</BaseLayout>
