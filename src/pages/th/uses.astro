---
import BaseLayout from '../../components/layout/BaseLayout.astro';
import UsesPage from '../../components/uses/UsesPage.astro';
import { t } from '../../i18n';

export const prerender = true;

const loc = 'th';
---

<BaseLayout
	title={t(loc, 'uses.title')}
	description={t(loc, 'uses.lead')}
	seoPath="uses"
>
	<UsesPage />
</BaseLayout>
