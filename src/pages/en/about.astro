---
import BaseLayout from '../../components/layout/BaseLayout.astro';
import AboutPage from '../../components/about/AboutPage.astro';
import { t } from '../../i18n';

export const prerender = true;

const loc = 'en';
---

<BaseLayout
	title={t(loc, 'about.title')}
	description={t(loc, 'about.lead')}
	seoPath="about"
>
	<AboutPage />
</BaseLayout>
